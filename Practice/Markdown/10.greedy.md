# Greedy

## 🟢 Easy

<details>
  <summary><span style="font-size:19px;"><strong>Assign Cookies - </strong><a href="https://leetcode.com/problems/assign-cookies/" target="_blank">#455</a></span></summary>

### 🧾 Input

* `g`: Children's greed factors
* `s`: Cookie sizes

### 📤 Output

* Maximum number of content children

### ✅ Example

```py
Input: g = [1,2,3], s = [1,1]
Output: 1
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def findContentChildren(self, g: List[int], s: List[int]) -> int:
        g.sort()
        s.sort()
        i = j = 0
        while i < len(g) and j < len(s):
            if s[j] >= g[i]:
                i += 1
            j += 1
        return i
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Gas Station - </strong><a href="https://leetcode.com/problems/gas-station/" target="_blank">#134</a></span></summary>

### 🧾 Input

* `gas`: Gas at each station
* `cost`: Cost to go to next station

### 📤 Output

* Starting station index (or -1 if not possible)

### ✅ Example

```py
Input: gas = [1,2,3,4,5], cost = [3,4,5,1,2]
Output: 3
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def canCompleteCircuit(self, gas: List[int], cost: List[int]) -> int:
        total, tank, start = 0, 0, 0
        for i in range(len(gas)):
            tank += gas[i] - cost[i]
            total += gas[i] - cost[i]
            if tank < 0:
                start = i + 1
                tank = 0
        return start if total >= 0 else -1
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Best Time to Buy and Sell Stock II - </strong><a href="https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/" target="_blank">#122</a></span></summary>

### 🧾 Input

* `prices`: Array of prices

### 📤 Output

* Max profit from any number of transactions

### ✅ Example

```py
Input: prices = [7,1,5,3,6,4]
Output: 7
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def maxProfit(self, prices: List[int]) -> int:
        profit = 0
        for i in range(1, len(prices)):
            if prices[i] > prices[i-1]:
                profit += prices[i] - prices[i-1]
        return profit
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Jump Game - </strong><a href="https://leetcode.com/problems/jump-game/" target="_blank">#55</a></span></summary>

### 🧾 Input

* `nums`: Maximum jump lengths

### 📤 Output

* Can reach last index (`True`/`False`)

### ✅ Example

```py
Input: nums = [2,3,1,1,4]
Output: True
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def canJump(self, nums: List[int]) -> bool:
        max_reach = 0
        for i, num in enumerate(nums):
            if i > max_reach:
                return False
            max_reach = max(max_reach, i + num)
        return True
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Lemonade Change - </strong><a href="https://leetcode.com/problems/lemonade-change/" target="_blank">#860</a></span></summary>

### 🧾 Input

* `bills`: List of 5, 10, or 20-dollar bills

### 📤 Output

* Can give change to all customers?

### ✅ Example

```py
Input: bills = [5,5,5,10,20]
Output: True
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def lemonadeChange(self, bills: List[int]) -> bool:
        five = ten = 0
        for bill in bills:
            if bill == 5:
                five += 1
            elif bill == 10:
                if five == 0: return False
                five -= 1
                ten += 1
            else:
                if ten > 0 and five > 0:
                    ten -= 1
                    five -= 1
                elif five >= 3:
                    five -= 3
                else:
                    return False
        return True
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Maximum Product of Three Numbers - </strong><a href="https://leetcode.com/problems/maximum-product-of-three-numbers/" target="_blank">#628</a></span></summary>

### 🧾 Input

* `nums`: List of integers

### 📤 Output

* Maximum product of any three numbers

### ✅ Example

```py
Input: nums = [1,2,3]
Output: 6
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def maximumProduct(self, nums: List[int]) -> int:
        nums.sort()
        return max(nums[-1] * nums[-2] * nums[-3], nums[0] * nums[1] * nums[-1])
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Walking Robot Simulation - </strong><a href="https://leetcode.com/problems/walking-robot-simulation/" target="_blank">#874</a></span></summary>

### 🧾 Input

* `commands`: List of movement and turn instructions
* `obstacles`: Coordinates of obstacles

### 📤 Output

* Maximum Euclidean distance squared from the origin

### ✅ Example

```py
Input: commands = [4,-1,3], obstacles = []
Output: 25
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def robotSim(self, commands: List[int], obstacles: List[List[int]]) -> int:
        dx = [0, 1, 0, -1]
        dy = [1, 0, -1, 0]
        x = y = direction = 0
        obstacle_set = set(map(tuple, obstacles))
        max_dist = 0

        for cmd in commands:
            if cmd == -1:
                direction = (direction + 1) % 4
            elif cmd == -2:
                direction = (direction - 1) % 4
            else:
                for _ in range(cmd):
                    nx, ny = x + dx[direction], y + dy[direction]
                    if (nx, ny) not in obstacle_set:
                        x, y = nx, ny
                        max_dist = max(max_dist, x*x + y*y)
        return max_dist
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n + m)
**Space:** O(m), where m = number of obstacles

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Merge Strings Alternately - </strong><a href="https://leetcode.com/problems/merge-strings-alternately/" target="_blank">#1768</a></span></summary>

### 🧾 Input

* `word1`, `word2`: Two strings

### 📤 Output

* A new string with alternating characters

### ✅ Example

```py
Input: word1 = "abc", word2 = "pqr"
Output: "apbqcr"
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def mergeAlternately(self, word1: str, word2: str) -> str:
        result = []
        for a, b in zip(word1, word2):
            result.append(a)
            result.append(b)
        result.append(word1[len(word2):])
        result.append(word2[len(word1):])
        return ''.join(result)
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(n)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Minimum Number of Steps to Make Two Strings Anagram - </strong><a href="https://leetcode.com/problems/minimum-number-of-steps-to-make-two-strings-anagram/" target="_blank">#1347</a></span></summary>

### 🧾 Input

* `s`, `t`: Two strings of equal length

### 📤 Output

* Minimum number of steps to make `t` an anagram of `s`

### ✅ Example

```py
Input: s = "bab", t = "aba"
Output: 1
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
from collections import Counter

class Solution:
    def minSteps(self, s: str, t: str) -> int:
        count_s = Counter(s)
        count_t = Counter(t)
        diff = count_s - count_t
        return sum(diff.values())
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(1) — fixed alphabet size (26 letters)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Strong Password Checker II - </strong><a href="https://leetcode.com/problems/strong-password-checker-ii/" target="_blank">#2299</a></span></summary>

### 🧾 Input

* `password`: A string

### 📤 Output

* Is the password strong?

### ✅ Example

```py
Input: password = "IloveLe3tcode!"
Output: True
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def strongPasswordCheckerII(self, password: str) -> bool:
        if len(password) < 8:
            return False

        has_lower = has_upper = has_digit = has_special = False
        special_chars = set("!@#$%^&*()-+")

        for i, ch in enumerate(password):
            if i > 0 and password[i] == password[i - 1]:
                return False
            if ch.islower(): has_lower = True
            elif ch.isupper(): has_upper = True
            elif ch.isdigit(): has_digit = True
            elif ch in special_chars: has_special = True

        return all([has_lower, has_upper, has_digit, has_special])
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(1)

</details>
</details>

## 🟡 Medium

<details>
  <summary><span style="font-size:19px;"><strong>Partition Labels - </strong><a href="https://leetcode.com/problems/partition-labels/" target="_blank">#763</a></span></summary>

### 📋 Input

* `s`: A lowercase string

### 📄 Output

* List of integers representing size of partitions

### ✅ Example

```py
Input: s = "ababcbacadefegdehijhklij"
Output: [9,7,8]
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def partitionLabels(self, s: str) -> List[int]:
        last = {ch: i for i, ch in enumerate(s)}
        j = anchor = 0
        res = []

        for i, ch in enumerate(s):
            j = max(j, last[ch])
            if i == j:
                res.append(i - anchor + 1)
                anchor = i + 1
        return res
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)  
**Space:** O(1) – constant alphabet size

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Jump Game II - </strong><a href="https://leetcode.com/problems/jump-game-ii/" target="_blank">#45</a></span></summary>

### 📋 Input

* `nums`: List of non-negative integers

### 📄 Output

* Minimum number of jumps to reach the last index

### ✅ Example

```py
Input: nums = [2,3,1,1,4]
Output: 2
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def jump(self, nums: List[int]) -> int:
        jumps = cur_end = cur_farthest = 0
        for i in range(len(nums) - 1):
            cur_farthest = max(cur_farthest, i + nums[i])
            if i == cur_end:
                jumps += 1
                cur_end = cur_farthest
        return jumps
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)  
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Candy - </strong><a href="https://leetcode.com/problems/candy/" target="_blank">#135</a></span></summary>

### 🧾 Input

* `ratings`: List of children's ratings

### 📤 Output

* Minimum number of candies to distribute

### ✅ Example

```python
Input: ratings = [1,0,2]
Output: 5
Explanation: You must give to the second child only 1 candy, then to the first child 2 candies and to the third child 2 candies.
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def candy(self, ratings: List[int]) -> int:
        n = len(ratings)
        candies = [1] * n

        for i in range(1, n):
            if ratings[i] > ratings[i - 1]:
                candies[i] = candies[i - 1] + 1

        for i in range(n - 2, -1, -1):
            if ratings[i] > ratings[i + 1]:
                candies[i] = max(candies[i], candies[i + 1] + 1)

        return sum(candies)
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(n)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Russian Doll Envelopes - </strong><a href="https://leetcode.com/problems/russian-doll-envelopes/" target="_blank">#354</a></span></summary>

### 🧾 Input

* `envelopes`: List of envelopes as `[width, height]`

### 📤 Output

* Maximum number of envelopes you can Russian doll

### ✅ Example

```python
Input: envelopes = [[5,4],[6,4],[6,7],[2,3]]
Output: 3
Explanation: The maximum number of envelopes you can Russian doll is 3 ([2,3] => [5,4] => [6,7]).
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
import bisect

class Solution:
    def maxEnvelopes(self, envelopes: List[List[int]]) -> int:
        envelopes.sort(key=lambda x: (x[0], -x[1]))
        dp = []

        for _, h in envelopes:
            idx = bisect.bisect_left(dp, h)
            if idx == len(dp):
                dp.append(h)
            else:
                dp[idx] = h

        return len(dp)
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log n)
**Space:** O(n)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Task Scheduler - </strong><a href="https://leetcode.com/problems/task-scheduler/" target="_blank">#621</a></span></summary>

### 🧾 Input

* `tasks`: A list of characters representing tasks (e.g., `["A","A","A","B","B","B"]`)
* `n`: Non-negative cooling interval between the same task

### 📤 Output

* Minimum time units to finish all tasks

### ✅ Example

```python
Input: tasks = ["A","A","A","B","B","B"], n = 2
Output: 8
Explanation: A -> B -> idle -> A -> B -> idle -> A -> B
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
from collections import Counter

class Solution:
    def leastInterval(self, tasks: List[str], n: int) -> int:
        freq = list(Counter(tasks).values())
        max_freq = max(freq)
        max_count = freq.count(max_freq)

        return max(len(tasks), (max_freq - 1) * (n + 1) + max_count)
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Minimum Number of Arrows to Burst Balloons - </strong><a href="https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/" target="_blank">#452</a></span></summary>

### 🧾 Input

* `points`: List of balloon intervals (start, end)

### 📤 Output

* Minimum number of arrows needed to burst all balloons

### ✅ Example

```python
Input: points = [[10,16],[2,8],[1,6],[7,12]]
Output: 2
Explanation: One arrow can burst [2,8],[1,6],[7,12]. Another for [10,16].
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def findMinArrowShots(self, points: List[List[int]]) -> int:
        points.sort(key=lambda x: x[1])
        arrows = 1
        end = points[0][1]

        for start, finish in points[1:]:
            if start > end:
                arrows += 1
                end = finish

        return arrows
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Jump Game VI - </strong><a href="https://leetcode.com/problems/jump-game-vi/" target="_blank">#1696</a></span></summary>

### 🧾 Input

* `nums`: List of integers
* `k`: Maximum number of steps you can jump forward

### 📤 Output

* Maximum score to reach the last index

### ✅ Example

```python
Input: nums = [1,-1,-2,4,-7,3], k = 2  
Output: 7
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
from collections import deque

class Solution:
    def maxResult(self, nums: List[int], k: int) -> int:
        dq = deque()
        dp = [0] * len(nums)
        dp[0] = nums[0]
        dq.append(0)

        for i in range(1, len(nums)):
            # Remove indices outside the window
            while dq and dq[0] < i - k:
                dq.popleft()
            dp[i] = nums[i] + dp[dq[0]]
            while dq and dp[i] >= dp[dq[-1]]:
                dq.pop()
            dq.append(i)

        return dp[-1]
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(n)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Split Array Into Consecutive Subsequences - </strong><a href="https://leetcode.com/problems/split-array-into-consecutive-subsequences/" target="_blank">#659</a></span></summary>

### 🧾 Input

* `nums`: Sorted list of integers

### 📤 Output

* Whether you can split the array into consecutive subsequences of length ≥ 3

### ✅ Example

```python
Input: nums = [1,2,3,3,4,5]  
Output: True
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
from collections import Counter, defaultdict

class Solution:
    def isPossible(self, nums: List[int]) -> bool:
        freq = Counter(nums)
        append_freq = defaultdict(int)

        for num in nums:
            if freq[num] == 0:
                continue
            if append_freq[num - 1] > 0:
                append_freq[num - 1] -= 1
                append_freq[num] += 1
            elif freq[num + 1] > 0 and freq[num + 2] > 0:
                freq[num + 1] -= 1
                freq[num + 2] -= 1
                append_freq[num + 2] += 1
            else:
                return False
            freq[num] -= 1

        return True
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n)
**Space:** O(n)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Largest Number - </strong><a href="https://leetcode.com/problems/largest-number/" target="_blank">#179</a></span></summary>

### 🧾 Input

* `nums`: List of non-negative integers

### 📤 Output

* Largest number formed by arranging elements

### ✅ Example

```python
Input: nums = [3,30,34,5,9]  
Output: "9534330"
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
from functools import cmp_to_key

class Solution:
    def largestNumber(self, nums: List[int]) -> str:
        nums = list(map(str, nums))
        
        def compare(x, y):
            return (int(y + x) - int(x + y))
        
        nums.sort(key=cmp_to_key(compare))
        result = ''.join(nums)
        return '0' if result[0] == '0' else result
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log n)
**Space:** O(n)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Koko Eating Bananas - </strong><a href="https://leetcode.com/problems/koko-eating-bananas/" target="_blank">#875</a></span></summary>

### 🧾 Input

* `piles`: List of banana piles
* `h`: Total hours to eat all bananas

### 📤 Output

* Minimum integer eating speed `k` such that all bananas are eaten in `h` hours

### ✅ Example

```python
Input: piles = [3,6,7,11], h = 8  
Output: 4
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
import math

class Solution:
    def minEatingSpeed(self, piles: List[int], h: int) -> int:
        left, right = 1, max(piles)

        while left < right:
            mid = (left + right) // 2
            total = sum(math.ceil(p / mid) for p in piles)
            if total > h:
                left = mid + 1
            else:
                right = mid
        return left
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log m), where m is max(piles)
**Space:** O(1)

</details>
</details>

## 🔴 Hard

<details>
  <summary><span style="font-size:19px;"><strong>Allocate Mailboxes - </strong><a href="https://leetcode.com/problems/allocate-mailboxes/" target="_blank">#1478</a></span></summary>

### 🧾 Input

* `houses`: List of house positions
* `k`: Number of mailboxes to place

### 📤 Output

* Minimum total distance between each house and its nearest mailbox

### ✅ Example

```python
Input: houses = [1,4,8,10,20], k = 3
Output: 5
Explanation: Allocate mailboxes at positions 1, 10 and 20.
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def minDistance(self, houses: List[int], k: int) -> int:
        houses.sort()
        n = len(houses)
        
        # Precompute cost[i][j]: min dist placing 1 mailbox from i to j
        cost = [[0]*n for _ in range(n)]
        for i in range(n):
            for j in range(i, n):
                m = houses[(i + j) // 2]
                cost[i][j] = sum(abs(h - m) for h in houses[i:j+1])

        # DP
        @lru_cache(None)
        def dp(i, k):
            if k == 0 and i == n:
                return 0
            if k == 0 or i == n:
                return float('inf')
            return min(cost[i][j] + dp(j+1, k-1) for j in range(i, n))
        
        return dp(0, k)
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n³) (can be optimized to O(n²k))
**Space:** O(n² + nk)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Minimum Number of Refueling Stops - </strong><a href="https://leetcode.com/problems/minimum-number-of-refueling-stops/" target="_blank">#871</a></span></summary>

### 🧾 Input

* `target`: Distance to reach
* `startFuel`: Initial fuel
* `stations`: List of fuel stations (position, fuel)

### 📤 Output

* Minimum number of refueling stops to reach the target, or -1 if impossible

### ✅ Example

```python
Input: target = 100, startFuel = 10, stations = [[10,60],[20,30],[30,30],[60,40]]
Output: 2
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
import heapq

class Solution:
    def minRefuelStops(self, target: int, startFuel: int, stations: List[List[int]]) -> int:
        pq = []
        stations.append([target, 0])
        fuel, prev, refuels = startFuel, 0, 0
        
        for location, capacity in stations:
            fuel -= location - prev
            while pq and fuel < 0:
                fuel += -heapq.heappop(pq)
                refuels += 1
            if fuel < 0:
                return -1
            heapq.heappush(pq, -capacity)
            prev = location
        return refuels
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log n)
**Space:** O(n)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Split Array Largest Sum - </strong><a href="https://leetcode.com/problems/split-array-largest-sum/" target="_blank">#410</a></span></summary>

### 🧾 Input

* `nums`: List of integers
* `k`: Number of subarrays to split into

### 📤 Output

* The minimum largest sum among these `k` subarrays

### ✅ Example

```python
Input: nums = [7,2,5,10,8], k = 2
Output: 18
Explanation: Split into [7,2,5] and [10,8], largest sum is 18.
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def splitArray(self, nums: List[int], k: int) -> int:
        def canSplit(mid):
            total, count = 0, 1
            for num in nums:
                if total + num > mid:
                    count += 1
                    total = num
                else:
                    total += num
            return count <= k
        
        left, right = max(nums), sum(nums)
        while left < right:
            mid = (left + right) // 2
            if canSplit(mid):
                right = mid
            else:
                left = mid + 1
        return left
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log(sum(nums)))
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Aggressive Cows – SPOJ (AGGRCOW)</strong></span></summary>

### 🧾 Input

* `stalls`: Positions of stalls (unsorted)
* `k`: Number of cows to place

### 📤 Output

* Maximum minimum distance between any two cows

### ✅ Example

```python
Input: stalls = [1, 2, 8, 4, 9], k = 3
Output: 3
```

### 🧠 Explanation

Place cows at positions 1, 4, and 8 — the minimum distance is 3.

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def aggressiveCows(self, stalls: List[int], k: int) -> int:
        stalls.sort()
        
        def canPlaceCows(distance):
            count, last_pos = 1, stalls[0]
            for i in range(1, len(stalls)):
                if stalls[i] - last_pos >= distance:
                    count += 1
                    last_pos = stalls[i]
                if count == k:
                    return True
            return False
        
        left, right, ans = 1, stalls[-1] - stalls[0], 0
        while left <= right:
            mid = (left + right) // 2
            if canPlaceCows(mid):
                ans = mid
                left = mid + 1
            else:
                right = mid - 1
        return ans
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log(max\_dist))
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Minimum Number of Arrows to Burst Balloons</strong> *(harder constraints)* - <a href="https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/" target="_blank">#452</a></span></summary>

### 🧾 Input

* `points`: List of balloon intervals `[start, end]`

### 📤 Output

* Minimum number of arrows to burst all balloons

### ✅ Example

```python
Input: points = [[10,16],[2,8],[1,6],[7,12]]
Output: 2
```

### 🧠 Explanation

You can shoot at x = 6 (covers \[2,8] and \[1,6]) and x = 12 (covers \[7,12] and \[10,16]).

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def findMinArrowShots(self, points: List[List[int]]) -> int:
        if not points:
            return 0
        points.sort(key=lambda x: x[1])
        arrows = 1
        end = points[0][1]
        
        for start, finish in points[1:]:
            if start > end:
                arrows += 1
                end = finish
        return arrows
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log n)
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Split Array into Fibonacci Sequence</strong> - <a href="https://leetcode.com/problems/split-array-into-fibonacci-sequence/" target="_blank">#842</a></span></summary>

### 🧾 Input

* `S`: A string of digits

### 📤 Output

* List of integers forming a valid Fibonacci-like sequence, or empty list

### ✅ Example

```python
Input: "123456579"
Output: [123, 456, 579]
```

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def splitIntoFibonacci(self, S: str) -> List[int]:
        def backtrack(index, path):
            if index == len(S) and len(path) >= 3:
                return path
            for i in range(index + 1, len(S) + 1):
                if S[index] == '0' and i > index + 1:
                    break
                num = int(S[index:i])
                if num > 2**31 - 1:
                    break
                if len(path) < 2 or path[-1] + path[-2] == num:
                    res = backtrack(i, path + [num])
                    if res:
                        return res
                elif len(path) >= 2 and path[-1] + path[-2] < num:
                    break
            return []
        
        return backtrack(0, [])
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(2ⁿ) worst case due to recursion
**Space:** O(n) call stack

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Maximize Sweetness of Chocolate</strong> - <a href="https://leetcode.com/problems/maximum-tastiness-of-candy-boxes/" target="_blank">#2517</a></span></summary>

### 🧾 Input

* `price`: List of prices of candies (treated as sweetness levels)
* `k`: Number of boxes (subsets) to pick

### 📤 Output

* Maximum possible *minimum difference* of sweetness between picked boxes

### ✅ Example

```python
Input: price = [13, 5, 1, 8, 21, 2], k = 3
Output: 8
```

### 🧠 Explanation

Choose chocolates such that difference between any two chosen is at least 8.

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def maximumTastiness(self, price: List[int], k: int) -> int:
        price.sort()
        
        def canSelect(min_diff):
            count = 1
            last = price[0]
            for i in range(1, len(price)):
                if price[i] - last >= min_diff:
                    count += 1
                    last = price[i]
                if count >= k:
                    return True
            return False
        
        left, right = 0, price[-1] - price[0]
        ans = 0
        while left <= right:
            mid = (left + right) // 2
            if canSelect(mid):
                ans = mid
                left = mid + 1
            else:
                right = mid - 1
        return ans
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n log(max\_diff))
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Minimum Cost to Cut a Stick</strong> - <a href="https://leetcode.com/problems/minimum-cost-to-cut-a-stick/" target="_blank">#1547</a></span></summary>

### 🧾 Input

* `n`: Total length of the stick
* `cuts`: List of positions to cut

### 📤 Output

* Minimum total cost to perform all cuts

### ✅ Example

```python
Input: n = 7, cuts = [1, 3, 4, 5]
Output: 16
```

### 🧠 Explanation

Use dynamic programming to find the best order of cuts.

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def minCost(self, n: int, cuts: List[int]) -> int:
        cuts = [0] + sorted(cuts) + [n]
        m = len(cuts)
        dp = [[0] * m for _ in range(m)]
        
        for l in range(2, m):
            for i in range(m - l):
                j = i + l
                dp[i][j] = min(dp[i][k] + dp[k][j] for k in range(i+1, j)) + cuts[j] - cuts[i]
        
        return dp[0][m - 1]
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n²)
**Space:** O(n²)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Find the Smallest Divisor Given a Threshold</strong> - <a href="https://leetcode.com/problems/find-the-smallest-divisor-given-a-threshold/" target="_blank">#1283</a></span></summary>

### 🧾 Input

* `nums`: List of integers
* `threshold`: Maximum allowed total sum after division

### 📤 Output

* The smallest possible divisor such that the **sum of all elements divided (rounded up)** is ≤ `threshold`.

---

### ✅ Example

```python
Input: nums = [1, 2, 5, 9], threshold = 6
Output: 5
```

### 🧠 Explanation

We want the smallest divisor such that:

* `ceil(1/d) + ceil(2/d) + ceil(5/d) + ceil(9/d) ≤ 6`
* Try divisors from 1 up to max(nums), use binary search to minimize.

---

<details>
  <summary><strong>Solution Code</strong></summary>

```python
import math

class Solution:
    def smallestDivisor(self, nums: List[int], threshold: int) -> int:
        left, right = 1, max(nums)

        while left < right:
            mid = (left + right) // 2
            total = sum(math.ceil(num / mid) for num in nums)
            if total > threshold:
                left = mid + 1
            else:
                right = mid

        return left
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n × log(max(nums)))
**Space:** O(1)

</details>
</details>

<details>
  <summary><span style="font-size:19px;"><strong>Valid Word Square</strong> *(Greedy + Backtracking)* - <a href="https://leetcode.com/problems/valid-word-square/" target="_blank">#422</a></span></summary>

### 🧾 Input

* `words`: List of strings

### 📤 Output

* Boolean value indicating if words form a valid word square

### ✅ Example

```python
Input: ["abcd", "bnrt", "crmy", "dtye"]
Output: True
```

### 🧠 Explanation

Each row and column must form the same word.

<details>
  <summary><strong>Solution Code</strong></summary>

```python
class Solution:
    def validWordSquare(self, words: List[str]) -> bool:
        for i in range(len(words)):
            for j in range(len(words[i])):
                if j >= len(words) or i >= len(words[j]) or words[i][j] != words[j][i]:
                    return False
        return True
```

</details>

<details>
  <summary><strong>Complexity Analysis</strong></summary>

**Time:** O(n²)
**Space:** O(1)

</details>
</details>

1. [Find the Smallest Divisor Given a Threshold](https://leetcode.com/problems/find-the-smallest-divisor-given-a-threshold/)
2. [Allocate Mailboxes](https://leetcode.com/problems/allocate-mailboxes/)
3. [Minimum Number of Refueling Stops](https://leetcode.com/problems/minimum-number-of-refueling-stops/)
4. [Split Array Largest Sum](https://leetcode.com/problems/split-array-largest-sum/)
5. [Aggressive Cows – Spoj or variant](https://www.spoj.com/problems/AGGRCOW/)
6. [Minimum Number of Arrows to Burst Balloons](https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/) *(harder constraints)*
7. [Split Array into Fibonacci Sequence](https://leetcode.com/problems/split-array-into-fibonacci-sequence/)
8. [Maximize Sweetness of Chocolate](https://leetcode.com/problems/maximum-tastiness-of-candy-boxes/)
9. [Minimum Cost to Cut a Stick](https://leetcode.com/problems/minimum-cost-to-cut-a-stick/)
10. [Valid Word Square](https://leetcode.com/problems/valid-word-square/) *(greedy+backtracking)*
